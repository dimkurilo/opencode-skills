# План проекта: ${PROJECT_NAME}

> Стратегический план. Статусы фаз и активные задачи — в `SESSION_HANDOFF.md`.
> Обновляется при принятии решений, изменении целей или добавлении фаз.
> Создано: ${DATE}

---

## Карта фаз

<!-- Граф зависимостей фаз. Формат: ASCII-дерево или mermaid. -->
<!-- Пример:
ФАЗА 0: ПОДГОТОВКА
├── 0.1 Сбор данных        → артефакт1        [требует участника]
├── 0.2 Анализ             → артефакт2        [независимая]
└── 0.3 Синтез             → результат         [ждёт 0.1 + 0.2]
-->

---

## Фаза N: Название

<!-- Для каждой фазы — отдельная секция. БЕЗ статуса выполнения. -->

**Цель:** ...

**Вход:**
- `путь/к/файлу` — описание

**Действия:**
1. ...
2. ...

**Выход:**
- `путь/к/артефакту` — описание

**Кто:** ... (агент / кандидат / совместно)

---

## Правила для всех фаз

1. Каждая фаза — отдельная сессия (новый чат)
2. Каждая фаза начинается с чтения: `plan.md` (этот файл), `SESSION_HANDOFF.md`, `.agents/memory/MEMORY.md`, `AGENTS.md`
3. Результат каждой фазы — конкретный файл (артефакт)
4. Артефакты НЕ переписываются следующей фазой — только читаются
5. Промпт для каждой фазы — короткий: «Выполни Фазу X согласно plan.md»
6. В конце каждой сессии агент обновляет: `SESSION_HANDOFF.md` (CURRENT_FOCUS, TASK_BACKLOG, ENVIRONMENT_NOTES, FILE:LINE_ANCHORS), `.agents/memory/MEMORY.md` (CONFIRMED_FACTS, UNRESOLVED_ISSUES, FAILED_APPROACHES, append-only)

---

## Заметки и решения

| Дата | Что | Решение |
|------|-----|---------|

---

## Блокеры

| Что | Как снять |
|-----|-----------|
