---
name: ${AGENT_NAME}
invoke: "@${AGENT_INVOKE}"
description: ${AGENT_DESCRIPTION}
---

# @${AGENT_INVOKE} — ${AGENT_ROLE}

**Роль:** ${AGENT_ROLE_DESCRIPTION}
**Вызывать когда:** ${AGENT_WHEN_TO_INVOKE}

---

## Что делает

${AGENT_RESPONSIBILITIES}

## Протокол

${AGENT_WORKFLOW}

## Output

${AGENT_OUTPUT_FORMAT}

## Пример

${AGENT_EXAMPLE}

---

## Примечание: `.opencode/agents/`

Если агенту нужна конкретная модель или temperature — используй шаблон `opencode-agent.md.tmpl` и размести файл в `.opencode/agents/`. Данный шаблон (`agent-persona.md.tmpl`) — для role-play персон без привязки к модели.
