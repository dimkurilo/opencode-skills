# ${API_SERVICE_NAME} — конфигурация API (пример)
# Скопируй в ${API_CONFIG_FILE} и заполни реальными значениями
# НИКОГДА не коммитить ${API_CONFIG_FILE} — только .example
# Формат определи из документации API: env, yaml, json, или plain text

${API_CONFIG_CONTENT}
