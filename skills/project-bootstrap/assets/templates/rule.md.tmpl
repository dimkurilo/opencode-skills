---
name: ${RULE_NAME}
description: ${RULE_DESCRIPTION}
applies_to: ${RULE_APPLIES_TO}
priority: ${RULE_PRIORITY}
${RULE_FRONTMATTER_EXTRA}
---

# ${RULE_TITLE}

${RULE_CONTENT}

## Gotchas / Типичные ошибки

<!-- Самая высокосигнальная часть правила. -->
<!-- Заполняется по мере обнаружения повторяющихся ошибок. -->
<!-- Пример: «Агент пытается редактировать файлы напрямую вместо использования специализированных скриптов — всегда используй scripts/ для детерминированных операций». -->
