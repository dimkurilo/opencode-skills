# Executor dispatch cheatsheet

Shape: **Task → Done (paths) → Autonomy**

```markdown
## Task
<verb + concrete result>

## Done when
- path/to/artifact

## Autonomy
- tools OK in scope
- no STATUS/MEMORY/SPEC edits
- 2× fail → Failure Packet
```

Anti: Done = "сделай хорошо" without path.
