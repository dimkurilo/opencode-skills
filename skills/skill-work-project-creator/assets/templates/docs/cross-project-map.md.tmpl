# Cross-project map — ${PROJECT_NAME}

| Adjacent project | Path | When to open |
|------------------|------|--------------|
| ${ADJ_1} | ${PATH_1} | ${WHEN_1} |

**Rule:** Do not mutate adjacent projects unless task explicitly owns them.
