# ASSUMPTIONS — ${SLUG}

> Interview log. Written when the user says "use defaults / решай сам" and the agent decides on their behalf.
> Every assumption = a decision the user did NOT make explicitly. Mirror these into the SPEC `<assumptions>` block.

## Source

- INTENT: `${INTENT_PATH}`
- Interview date: ${DATE}
- User said: <!-- "use defaults" / "решай сам" / the explicit answer -->

## Assumptions

| ID | Question | Default chosen | Why (evidence) | Risk if wrong |
|----|----------|----------------|----------------|---------------|
| AS1 | <open question> | <recommended default> | <why this default> | <what breaks if wrong> |

## Confirmed by user (not assumptions)

- <answers the user gave explicitly — these are decisions, not assumptions>

## Open (still needs user)

- <questions that still block SPEC; none → "None.">
