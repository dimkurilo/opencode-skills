# REVIEW-STAMP — ${WAVE_ID}

```
AGREED: NO
SPEC_PATH: SPEC.md
PLAN_PATH: PLAN.md
SPEC_HASH: 
ROUND: 1
MAX: 5
SESSION_ID: ${SESSION_ID}
CHECKLIST_VERSION: ${CHECKLIST_VERSION}
TIMESTAMP: ${DATE}
AUTHORITY: Wave Reviewer only (not executor)
```

## Open deltas (if AGREED=NO)

| id | path | fix | severity |
|----|------|-----|----------|
| D1 | | | |

## Execute blockers
- (list or "none after YES")

## Next action for Author
- Patch only open deltas; re-dispatch **same review session**.

## Notes
- On YES: run `hash_acceptance.sh <wave_dir>`, put **64-hex** into `SPEC_HASH` (placeholder text fails schema).
- Before EXECUTE: `verify_stamp_schema.sh` + `verify_stamp_hash.sh` (live files must match stamp hash).
- Fresh **executor** session after YES + verified hash (not the review session).
- Do not expand checklist mid-loop except CRITICAL safety.
- Forbidden: claim AGREED in chat if this file says NO.
- Executor must not re-write this stamp or re-hash to self-approve.
