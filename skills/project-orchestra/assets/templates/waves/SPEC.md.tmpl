# SPEC — wave ${WAVE_ID}

> Markdown wave contract (equal to SPEC.xml for stamp + hash).  
> Prefer this when wave-spec is not installed.  
> Copy into a **live** `waves/<date-slug>/` dir — do not treat `waves/_template/` as a wave.

| Field | Value |
|-------|-------|
| wave | ${WAVE_ID} |
| date | ${DATE} |
| workstream / parent | ${PARENT_OR_WS} |

## Intent

${GOAL}

## In scope

- ${STEP_1}

## Out of scope

- ${OUT_OF_SCOPE}

## Acceptance (done when)

- ${DONE_1}
- Stamp `AGREED: YES` + matching hash via `hash_acceptance.sh` / `verify_stamp_hash.sh`

## Dual-review

| dual-review | required \| optional \| DEGRADED_DUAL \| waived |
|-------------|------------------|
| notes | |

## Notes

- MD = XML for stamp + hash
- Reviewer session for rounds 1..N; fresh executor session after YES + verified hash
