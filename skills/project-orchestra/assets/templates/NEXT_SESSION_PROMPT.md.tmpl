# Next session prompt — ${PROJECT_NAME}

Copy into a **new** agent session (new session per wave after stamp YES).

---

## Task

${TASK_SUMMARY}

## Context (read first, ≤5 files)

1. `SESSION_HANDOFF.md` — last block only
2. `STATUS.md` (parent) or workstream `STATUS.md` if focus is a slug
3. `.agents/memory/MEMORY.md`
4. Wave dir: `${WAVE_DIR}` (SPEC + PLAN + REVIEW-STAMP)
5. Optional: `${EXTRA_FILE}`

## Mode

- Skill: **project-orchestra**
- Mode: `${MODE}`  (e.g. raeh-execute / wave / workstream-new)
- CWD: `${PROJECT_DIR}`

## Constraints

- Executor does **not** edit STATUS / MEMORY / HANDOFF / AGENTS
- EXECUTE only if stamp `AGREED: YES` and hash matches
- No domain hostnames inventing; evidence paths required
- Done = artifacts + verify, not vibes

## Done

- [ ] `${DONE_CRITERION_1}`
- [ ] EXEC-REPORT.md with evidence paths (if execute)
- [ ] Orchestrator handoff append (coordinator, not executor)

## Autonomy

${AUTONOMY_NOTES}
