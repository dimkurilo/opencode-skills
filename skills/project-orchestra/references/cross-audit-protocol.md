# F-04 Cross-audit (cost-controlled)

| Situation | Depth |
|-----------|-------|
| L0 CLEAN + inventory clear | Skip multi-model architecture panel |
| L0 CONTRADICTIONS | 2-family minimum on conflict only |
| FIRST high-stakes / human asks | Up to 3–4 models once |
| Wave-level SPEC | Never full panel — Stamp Dialogue |
| After important synthesis | Different family F-04 on claims |
| Gate-critical skill/package ship | Dual: F-04 ledger + stress (see production-playbook) |

Auditor uses `auditor-what-where` profile. Output under `audits/<auditor>/`.

## Complementary dual (1.1)

| Lane | Prompt job |
|------|------------|
| F-04 ledger | Checklist, evidence paths, acceptance criteria |
| Stress | Failure modes, missing gates, rollback |

Pin agent+model; identity gate; invalid archive — `dispatch-iron.md`.  
Transport: Orca dual terminals — `orca-recipes.md`.  
If dual required but unavailable: `DEGRADED_DUAL` — `degraded-modes.md`.
